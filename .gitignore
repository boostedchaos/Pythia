.venv/
__pycache__/
*.pyc
.env
runs/*.jsonl
*.pem
.DS_Store
# raw screen recording — the compressed screenshots/demo.mp4 is committed instead
Screen Recording.mov
*.mov

# nested Osiris checkout (own git repo)
osiris-live/

# local Claude Code session settings
.claude/settings.local.json

# playwright MCP session artifacts (logs/screenshots) — never commit
.playwright-mcp/

# compose secrets
deploy/compose/.env
